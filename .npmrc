# pnpm settings for the ParleyNotes monorepo.
# node-linker=hoisted keeps a flat node_modules that Vite/Next/Tauri tooling and
# native addon resolution handle most predictably in a mixed JS + Rust workspace.
node-linker=hoisted
shamefully-hoist=true
