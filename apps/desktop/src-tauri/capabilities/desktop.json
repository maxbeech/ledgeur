{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only capabilities: the auto-updater and relaunch. The phone stores handle updates, and the plugins are not compiled in there (see Cargo.toml).",
  "windows": ["main"],
  "platforms": ["macOS", "windows", "linux"],
  "permissions": [
    "updater:default",
    "process:default"
  ]
}
