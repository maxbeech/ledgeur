// Single source of truth for external links used by the app.
const SITE = import.meta.env.VITE_SITE_URL ?? "https://parleynotes.com";
export const SITE_PRICING_URL = `${SITE}/pricing`;
export const SITE_URL = SITE;
